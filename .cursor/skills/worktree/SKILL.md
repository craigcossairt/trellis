---
name: worktree
description: Set up parallel agent sessions on independent tasks via git worktrees. Use when asked to 'use a worktree', 'work on two things at once', 'isolated branch workspace', or before starting branch work alongside another session.
---

Cursor does not auto-load Claude Code skills, so this is a router. The procedure lives in one
place and every harness points at it - never fork the body into this file.

Read `.claude/commands/worktree.md` and follow it before creating the branch, not after.
