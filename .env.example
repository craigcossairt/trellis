# Copy to .env and fill from your password manager. Never commit .env.
# Add one line per secret the project needs, with a comment saying where it comes from.

# Example:
# DATABASE_URL=            # password manager entry: "<project> - database"
# API_KEY=                 # password manager entry: "<project> - api key"
